# integrated: frontend e API compartilham a mesma origem.
# local-runtime: o frontend publicado acessa o runtime desta máquina.
VITE_APP_MODE=integrated

# Deixe vazio no modo integrated. Nunca coloque segredos em VITE_*.
VITE_API_URL=
