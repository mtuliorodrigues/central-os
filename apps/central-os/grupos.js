import{localApiFetch}from"/local-api.js";const l=document.getElementById("groupsList"),b=document.getElementById("groupsCount");try{const d=await localApiFetch("/api/grupos");const g=d.groups||[];b.textContent=`${g.filter(x=>x.available).length} disponível(is)`;l.innerHTML=g.map(x=>`<article class="card"><h3>${x.name||"Grupo"}</h3><p>${x.available?"Disponível para consulta":"Indisponível nesta sessão"}</p></article>`).join("");}catch(e){b.textContent="Indisponível";l.innerHTML=`<div class="empty">${e.message}</div>`;}
