# Relatório OS — runtime integrado

Cópia isolada do runtime atual. O arquivo `src/motor_relatorio_os.py` deve permanecer sem alterações de regra de negócio.

Os arquivos reais `grupos_relatorio_os.json`, `usuarios_relatorio.json` e `.env` são dados locais e não devem ser commitados.
